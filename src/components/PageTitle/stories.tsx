import { Story, Meta } from '@storybook/react/types-6-0'
import PageTitle from '.'

export default {
  title: 'PageTitle',
  component: PageTitle
} as Meta

export const Default: Story = () => <PageTitle />
