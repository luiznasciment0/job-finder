import { Story, Meta } from '@storybook/react/types-6-0'
import HeroSearch from '.'

export default {
  title: 'HeroSearch',
  component: HeroSearch
} as Meta

export const Default: Story = () => <HeroSearch />
